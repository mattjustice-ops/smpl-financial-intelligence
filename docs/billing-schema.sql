-- Reference schema for SMPL Stripe billing (managed by Alembic migration billing_001)
-- Table names: billing_customers maps to spec "customers", billing_checkout_sessions maps to "checkout_sessions"

-- organizations (extended)
--   slug, status, plan, stripe_customer_id, stripe_subscription_id, trial_ends_at, updated_at

-- billing_customers
-- billing_subscriptions
-- billing_events
-- billing_checkout_sessions
-- pending_user_invites
